# D4 S15 reference page ("Hell's Legacy Lexicon")

- Repo `tectiv3/runewords`, deployed at https://tectiv3.github.io/runewords/ (GitHub Pages, `main` root, push-to-deploy). NOTE: after push, old build serves for ~15–60s until Pages rebuild completes — verify content with retry loop, not a single curl.
- Three tabs: Runewords (19 cube runewords) | Runes (16, inventory tally) | Uniques (10 Legacy Uniques). Search query deliberately persists across tabs (tests must clear it before switching).
- Single-file `index.html`, vanilla JS/CSS, no deps. Editing source of truth = `data.json` (repo root, byte-identical to the embedded `<script id="rw-data" type="application/json">` block). Update workflow: edit `data.json`, run `python3 scripts/sync.py` (embeds; asserts round-trip), run `python3 scripts/verify.py` (sync + invariants check), commit, push. Never hand-edit the embedded block. Footer stamp (`asof` field) must be bumped with data changes. Invariants live in `scripts/rwdata.py` (shared by sync/verify): 19 runewords, 16 runes, unique names, name+slot+effect on all uniques, `drop` required except cls=Legacy (Legacy acquisition is the generic `uniquesnotes.acquisition` banner), cls typo guard (≥2 uniques per cls value; set derived dynamically as classes grow).
- Data policy: consensus of d4guides.gg + Sportskeeda (patch 3.2.1 datamine, normal-tier recipes; ancestral ≈ 2×). aoeah.com's article (original source) has multiple errors: ~2× values, wrong Ancient's Pledge/Zephyr stats, wrong base lists. `|?` suffix in effect strings = unconfirmed (renders ⚠).
- Spec + critic findings: `docs/plans/spec.md`.
- Verification harness: jsdom runtime test at /tmp/rwtest/domtest.js (25 assertions) — page is JS-rendered, so grep for static markers only (title, `id="rw-data"`), never `data as of` (client-rendered).
- Legacy Uniques acquisition (corrected via game8, user-initiated deep pass): GENERIC, not item-specific — Waking Nightmare Prime Evil capstones (Echo of Baal/Mephisto/Diablo, T X+ empowered ≈ T XII), rarely Chronicle of Nightmares minor bosses; Echo of Pindleskin + Uber Tristram may drop anything. Godly Plate of the Whale: no confirmed source. d4guides' earlier "world drop, no boss source" was a PTR-era disclaimer (Sep 6) — their post-launch articles corroborate game8. 5 uniques carry dispute markers (Leoric, Squirt, Arioc, Furnace, Messerschmidt 20–30% vs game8 40–60%).
- Verification harness: jsdom runtime tests at /tmp/rwtest/ — domtest.js + uqfeat.js + mythictest.js + universaltest.js (178 assertions total: regressions, tooltip+slot-chips, mythic slice, universal slice). Page is JS-rendered, so grep for static markers only (title, `id="rw-data"`), never `data as of` (client-rendered). Worker agents' own fake-DOM harnesses are not a substitute for these jsdom runs.

- Slice progress: Legacy 10 + Rogue 32 + Mythic 13 + Universal 36 done (All = 91). Remaining: Barb 28, Sorc 36, Necro 38, Spiritborn 23. Warlock done (38; 6 disputes). Bosses tab live (19 entries; loot via drop-field match substring; Belial/Mephisto/Countess/Pindleskin are note-only). Druid done (36; Tempest Roar & Vasily Prayer carry literal (PH) placeholder effect text in 3.2.1). Paladin done (22; Lightburst Hammer excluded — dead DB entry like Demon-Eye).. Per-slice acquisition banners are ONE div `#uqbanner`, driven by the `UQ_SLICE_NOTES` map (slice → `uniquesnotes` key: Legacy→acquisition, Mythic→mythicAcq, Universal→universalAcq; text set per-render in renderUniques); charms footnote stays Legacy-only. Slice chips + slot chips are data-driven (add cls-tagged data + map entry if the slice needs a banner, UI auto).
